kind = "web"
previewPath = "/"
title = "Expiry Scan App"
version = "1.0.0"
id = "artifacts/expiry-scan-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21562

[services.development]
run = "pnpm --filter @workspace/expiry-scan-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/expiry-scan-app", "run", "build" ]
publicDir = "artifacts/expiry-scan-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21562"
BASE_PATH = "/"
